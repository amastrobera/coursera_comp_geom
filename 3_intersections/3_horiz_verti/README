1. Verify whether horizontal / vertical segments intersect 


outputs =  "A common segment of non-zero length." 
           "The intersection point is (x, y)."
           "No common points."

**requirements**
language: C++ (11) 
OS: ubuntu 18.04
compiler: g++ v 7.4.0 (lower version is also fine)

**compile with**
```
g++ -pipe -O2 -std=c++14 main.cpp -lm   # will output a.out, like coursera wants
```

**launch with**
```
cat data1.txt | ./a.out
cat data2.txt | ./a.out
```
or with 
```
for f in $(ls data*.txt); do echo "$f:  $(cat $f | ./a.out)"; done
```